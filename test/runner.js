chai     = require( 'chai' );
util     = require( 'useful-util' );
Observer = require( '../index' );
expect   = chai.expect;

require( './Observer.test.js' );
require( './Observer.Callback.test.js' );
